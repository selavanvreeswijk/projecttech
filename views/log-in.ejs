<!DOCTYPE html>
<html lang="en ">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log-in</title>
    <link rel="stylesheet" href="/style.css">

    <%- include('partials/header') %>    

    <style>
        body{
            display: flex;
            flex-direction: column;
        }


        .container{
            margin: 0.5em 1.5em;
        }


        .log-in{
            display: flex;
            flex-direction: column;
        }


        h2{
            border-bottom: 0.05em solid var(--color-lines);

            width: 11.3em;
            padding-bottom: 0.2em;
        }

        
        label{
            margin-top: 10em;
            margin-bottom: 0.5em;
        }


        input{
            margin-top: 0.3em;
        }


        form div{
            margin-top: 1.5em;
        }


        button{         
            display: block;
            margin: 0 auto;
            margin-top: 2em;
        }

    
        .register{
            display: flex;
            flex-direction: column;
            align-items: center;

            margin-top: 3em;
        }


        .register a{
            margin-top: 1em;
            padding: 0.2em 0.8em;
            font-size: 0.8em;
        }


        /* Error */
        .error{
            border: 2px solid red !important;
            color: red !important;
        }

        .error-message{
            display: none;

            color: red;
            font-size: 14px;
            margin-bottom: 10px;
        }
    </style>
    </head>

    <body>
        <div class="container">
            <div class="log-in">
                <h2>Login</h2>
    
                <form action="/log-in" method="POST">
                    <div>
                        <label for="username">Username</label>
                        <input class="form-input-styling" type="text" name="username" required>
                    </div>
         
                    <div>
                        <label for="password"> Password</label>
                        <input class="form-input-styling" type="password" name="password" required>
                    </div>

                    <p class="error-message" id="error-msg">Invalid username or password, try again</p>
     
                    <button class="btn-styling" type="submit">Log In</button>
                </form>
            </div>
    
            <div class="register">
                <p>Don't have an account?</p>

                <a class="btn-styling" href="/register">Register</a>
            </div>
        </div>
    
    <footer>
        <%- include('partials/footer') %> 
    </footer>

    <script>
        document.getElementById("login-form").addEventListener("submit", async function(event) {
            event.preventDefault(); 

            let username = document.getElementById("username").value;
            let password = document.getElementById("password").value;
            let errorMsg = document.getElementById("error-msg");

            try {
                let response = await fetch("http://localhost:9000/log-in", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ username, password })
                });

                let data = await response.json();

                if (response.ok) {
                    alert("Login successful!"); 
                    window.location.href = "/dashboard";
                } else {
                    errorMsg.style.display = "block";
                }
            } catch (error) {
                console.error("Error:", error);
            }
        });
    </script>

    </body>
</html>